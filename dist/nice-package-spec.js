'use strict';
/* eslint-env mocha */
var nicePackage = require('.');
describe('@bahmutov/nice-package', function () {
    it('write this test', function () {
        console.assert(nicePackage, 'should export something');
    });
});
