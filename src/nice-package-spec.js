'use strict'

/* eslint-env mocha */
const nicePackage = require('.')

describe('@bahmutov/nice-package', () => {
  it('write this test', () => {
    console.assert(nicePackage, 'should export something')
  })
})
